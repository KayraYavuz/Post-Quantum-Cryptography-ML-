apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pqc-gateway-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: pqc-gateway.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pqc-gateway-service
            port:
              number: 80
