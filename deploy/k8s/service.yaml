apiVersion: v1
kind: Service
metadata:
  name: pqc-gateway-service
  namespace: default
  labels:
    app: pqc-gateway
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8090
    protocol: TCP
    name: http
  selector:
    app: pqc-gateway
